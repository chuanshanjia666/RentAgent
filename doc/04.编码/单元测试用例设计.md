# RentAgent 单元测试用例设计

> 项目名称：RentAgent —— 基于 AI 智能体的房屋租赁系统
> 文档版本：v1.0
> 编制日期：2026-09-17
> 编制：项目组测试担当（陈玄晔）
> 关联文档：《需求分析矩阵》v1.1（FR-01~25 / NFR-01~10）、《需求跟踪矩阵》v1.1、《概要设计》v1.2、《数据库表》、<br>《测试用例设计》（集成与端到端层）、`scripts/ci-smoke.sh`、`.github/workflows/ci.yml`
> 适用阶段：四、编码（单元测试）

---

## 一、测试范围与分层策略

单元测试只覆盖**不依赖外部进程**的逻辑：状态机、权限归属、金额与日期计算、文本解析、加解密、参数约束。
凡属"必须真连 MySQL / Redis / 网络才能验证"的行为（唯一键约束、逻辑删除、`JSON_CONTAINS` 检索、聚合 SQL、
验证码 TTL、模型流式输出）不在本层做假测试，统一交给《测试用例设计》的集成层，
避免用 Mock 造出与真实语义不符的"绿色假象"。

| 层次 | 手段 | 依赖 | 用例数 | 执行命令 |
| ---- | ---- | ---- | ------ | -------- |
| 后端单元测试 | JUnit 5 + Mockito 打桩 Mapper | 无（不连库/缓存/网络） | **148** | `cd server && mvn test` |
| 前端单元测试 | vitest + jsdom | 无（假 `fetch`/`ReadableStream`） | **20** | `cd frontend && npm test` |
| 集成与端到端 | 真实 MySQL 8 + Redis + 运行中后端 | 全栈 | **95 条断言** | `bash scripts/ci-smoke.sh` |

其中后端 148 条 = 既有 9 条（`AiPropsTest` 6、`OpenAiResponsesChatModelTest` 3）+ 本次新增 **139 条**；
前端 20 条全部为本次新增。

### 1.1 测试目录结构

```
server/src/test/java/com/rentagent/
├── agent/OpenAiResponsesChatModelTest.java   # 既有：Responses 协议请求映射与响应解析
├── config/AiPropsTest.java                   # 既有：多后端（协议）解析优先级
├── common/ApiContractTest.java               # 统一响应体与全局异常处理
├── common/CryptoUtilTest.java                # 身份证 AES-GCM 与 SHA-256
├── dto/DtoValidationTest.java                # 请求参数 Bean Validation 约束
├── security/SecurityGuardTest.java           # JWT 签发/篡改/过期、角色拦截器、线程上下文
├── service/HouseServiceTest.java             # FR-05/06/07/11
├── service/AppointmentServiceTest.java       # FR-17
├── service/ContractServiceTest.java          # FR-18/19
├── service/ReviewServiceTest.java            # FR-20
├── service/AuthServiceTest.java              # FR-01/02/03
├── service/AnalysisServiceTest.java          # FR-08/14/15/16
├── service/KbServiceTest.java                # FR-13
└── support/EntityMetadataHelper.java         # 测试辅助：注册 MyBatis-Plus 实体元数据

frontend/src/
├── runtime.test.ts                           # 一套代码两种分发的地址解析
├── constants.test.ts                         # 状态字典与格式化
└── api.test.ts                               # AI 对话 SSE 流式解析
```

### 1.2 测试替身的使用边界

| 替身对象 | 处理方式 | 理由 |
| -------- | -------- | ---- |
| MyBatis-Plus Mapper | Mockito `@Mock` 打桩 | 单测不校验 SQL，只校验"是否发起正确的调用与状态迁移" |
| `AuthService.realnamePassed` | Mockito 打桩 | 隔离 FR-03 依赖，专测 FR-05 的实名校验分支 |
| `StringRedisTemplate` | Mockito 打桩（含 `opsForValue()` 链） | 验证键名、写入值、TTL 与失败计数语义 |
| BCrypt / AES / SHA-256 | **真实实现** | 加密强度与格式属于被测行为，不能替身 |
| `NotificationService` | Mockito 打桩 | 验证"该不该发、发给谁、什么类型" |
| 数据库唯一键冲突 | `thenThrow(new DuplicateKeyException(...))` | 单测无法造唯一键，只能注入其异常以验证业务码映射 |
| 前端 `fetch` / `ReadableStream` | `vi.stubGlobal` + 内存流 | 精确控制分片边界（半行缓冲）与失败分支 |
| 前端 `localStorage` | 测试内内存版 Storage 实现 | vitest 的 jsdom 环境不注入 `localStorage` |
| `window.RentAgent`（桌面端注入点） | `vi.stubGlobal` + `vi.resetModules()` 后动态导入 | 模块级读取注入值，需按用例重建模块 |

### 1.3 一个必要的测试基础设施

纯 Mockito 单测没有 MyBatis 容器初始化实体缓存，一旦被测代码需要**渲染 lambda 列名**
（`LambdaUpdateWrapper.set(...)`、`Wrapper.getSqlSegment()`，如房源评分回填、订单/合同列表过滤条件），
MyBatis-Plus 会抛 `can not find lambda cache for this entity`。
`support/EntityMetadataHelper` 按需调用 `TableInfoHelper.initTableInfo(...)` 注册实体元数据，
使这些断言可以校验真实列名（如 `tenant_id = ?`），而不是绕开验证。

---

## 二、后端单元测试用例

约定：`预期` 列中的 `xxxx` 为统一响应体 `code`（业务错误码）；所有业务错误在 HTTP 层均为 200，
错误只体现在响应体 `code` 字段（唯一例外是 `JwtAuthFilter` 对非法/过期 token 直接返回 HTTP 401）。

### 2.1 UT-AUTH 认证与用户（FR-01/02/03）— `AuthServiceTest`，13 条

| 编号 | 被测规则 | 输入 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- | ---- |
| UT-AUTH-01 | 注册须验证码正确且未过期 | 验证码不符 / Redis 中无验证码 | `1005`，且不写用户 | FR-01 |
| UT-AUTH-02 | 手机号唯一 | 该手机号已有用户 | `1001` | FR-01 |
| UT-AUTH-03 | 注册成功自动登录 | 验证码正确、手机号未注册 | 用户名=手机号、默认昵称取后 4 位、`status=1`、密码 BCrypt、验证码删除、返回 token | FR-01 |
| UT-AUTH-04 | 昵称兜底 | 昵称为空白串 | 落库昵称为 `用户<后4位>` | FR-01 |
| UT-AUTH-05 | 登录成功清计数 | 密码正确 | 返回 token，`login:fail:<用户名>` 被删除 | FR-02 |
| UT-AUTH-06 | 密码错误计数 | 密码错误 | `1002`，计数自增且 TTL=10 分钟 | FR-02 |
| UT-AUTH-07 | 不泄漏账号是否存在 | 用户不存在 | 与密码错误同为 `1002` | FR-02 |
| UT-AUTH-08 | 连续失败锁定 | 计数已达 5 | `1003`，且不再比对密码（不查库） | FR-02（验收：5 次锁定 10 分钟） |
| UT-AUTH-09 | 禁用账号不可登录 | `status=0` | `1004` | FR-22 |
| UT-AUTH-10 | 实名提交双写 | 姓名 + 18 位身份证 | AES-GCM 密文（≠明文）+ 64 位 SHA-256，`status=0` | FR-03 / NFR-04 |
| UT-AUTH-11 | 实名通过判定口径 | 存在/不存在 `status=1` 记录 | 存在性判定（任一通过即放行） | FR-03 |
| UT-AUTH-12 | 实名查询脱敏 | 有/无认证记录 | 无记录返回 `null`；有记录返回 `2101**********1234` 形式 | FR-03 / NFR-04 |
| UT-AUTH-13 | 演示验证码 | 发送验证码 | 写入 `captcha:<手机号>` = `246810`，TTL 5 分钟 | FR-01 |

### 2.2 UT-HOUSE 房源（FR-05/06/07/11）— `HouseServiceTest`，19 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-HOUSE-01 | 未实名房东发布房源 | `1008`，不落库 | FR-03/FR-05 |
| UT-HOUSE-02 | 实名房东发布 | `status=0`（待审核）、`viewCount=0`、房东=当前用户、设施 JSON 化、图片按序入库首图为封面 | FR-05 |
| UT-HOUSE-03 | 编辑房源 | 状态强制回到 `0` 且清空驳回原因 | FR-06 |
| UT-HOUSE-04 | 非房主编辑 | `1007` | NFR-03 |
| UT-HOUSE-05 | 上架合法前置 | 已通过 `1` / 已下架 `4` → 已上架 `3` | FR-06 |
| UT-HOUSE-06 | 下架合法前置 | 已上架 `3` → 已下架 `4` | FR-06 |
| UT-HOUSE-07 | 非法迁移 | 待审核/已驳回/已出租上架、非上架下架、重复下架 → `2002` | FR-06 |
| UT-HOUSE-08 | 未知动作 | `1000` | FR-06 |
| UT-HOUSE-09 | 非房主上下架 | `1007` | NFR-03 |
| UT-HOUSE-10 | 房源不存在 | `2001`（上下架/编辑） | FR-06 |
| UT-HOUSE-11 | 审核结果 | 通过 → `1` 且清空驳回原因；驳回 → `2` 且写入理由 | FR-07 |
| UT-HOUSE-12 | 重复审核 | 非待审核状态一律 `2002` | FR-07 |
| UT-HOUSE-13 | 审核不存在房源 | `2001` | FR-07 |
| UT-HOUSE-14 | 匿名可见性 | 仅 `3`/`5` 可见，其余伪装为 `2001` | FR-07/FR-09（"仅已通过房源可被检索"） |
| UT-HOUSE-15 | 房东/管理员可见性 | 角色 2/3 可查看任意状态 | FR-07 |
| UT-HOUSE-16 | 详情聚合 | 浏览计数走 SQL 自增；收藏状态与评价数按当前访问者返回；图片顺序与封面标记 | FR-11 |
| UT-HOUSE-17 | 设施 JSON 容错 | 序列化/反序列化往返；`null` 与坏 JSON 均返回空列表不抛错 | FR-05 |
| UT-HOUSE-18 | 推荐位候选集 | 只取已上架房源；匿名按热度返回；不查收藏偏好 | FR-11 |
| UT-HOUSE-19 | 推荐偏好加权 | 收藏所在行政区权重（×10）+ 热度 + 评分参与排序 | FR-11 |

### 2.3 UT-APPT 看房预约（FR-17）— `AppointmentServiceTest`，12 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-APPT-01 | 仅已上架房源可预约 | 房源不存在或状态非 `3` → `2001` | FR-17 |
| UT-APPT-02 | 不能预约自己的房源 | `1000` + 文案「不能预约自己的房源」 | FR-17 |
| UT-APPT-03 | 创建成功 | `status=0`（待确认），房东由房源冗余写入，通知房东（type=1） | FR-17/FR-21 |
| UT-APPT-04 | 同时段冲突 | `3001`，且**不发送**通知 | FR-17（验收：时段冲突禁止重复预约） |
| UT-APPT-05 | 唯一键口径固化 | `uk_house_time` 不含状态：已取消/已拒绝的历史预约仍占用该时段 → `3001` | FR-17 |
| UT-APPT-06 | 预约不存在 | `3002` | FR-17 |
| UT-APPT-07 | 房东确认 | 仅待确认可确认；非房东 `1007`；已确认再确认 `3002`；成功后通知租客 | FR-17 |
| UT-APPT-08 | 房东拒绝 | 写入拒绝原因并通知租客 | FR-17/FR-21 |
| UT-APPT-09 | 租客取消 | 待确认/已确认可取消；已拒绝/已完成/已取消 `3002`；房东越权 `1007` | FR-17 |
| UT-APPT-10 | 完成看房 | 必须已确认（`1`→`3`）；待确认直接完成 `3002` | FR-17（验收：状态流转） |
| UT-APPT-11 | 未知动作 | `1000` 且不落库 | FR-17 |
| UT-APPT-12 | 列表隔离 | 租客侧按 `tenant_id`、房东侧按 `landlord_id` 过滤，并组装房源标题/封面/租金 | FR-17 |

### 2.4 UT-CONTRACT 签约、订单与账单（FR-18/19）— `ContractServiceTest`，20 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-CONTRACT-01 | 仅已上架房源可签约 | `2001` | FR-18 |
| UT-CONTRACT-02 | 不能与自己的房源签约 | `1000` | FR-18 |
| UT-CONTRACT-03 | 租期合法性 | 起止无序/起止相同/起始早于今天 → `1000` | FR-18 |
| UT-CONTRACT-04 | 起始日为今天 | 允许（边界） | FR-18 |
| UT-CONTRACT-05 | 防重复发起 | 同房源同租客已存在进行中合同 → `3003`，不落库 | FR-18 |
| UT-CONTRACT-06 | 创建成功 | `status=0`，月租与押金取房源租金，条款 JSON 含双方与小区信息，通知房东（type=3） | FR-18/FR-21 |
| UT-CONTRACT-07 | 合同不存在 | `3004` | FR-18 |
| UT-CONTRACT-08 | 租客签署 | `0`→`1`，写 `signedTenantAt`，通知房东 | FR-18 |
| UT-CONTRACT-09 | 房东签署 | `1`→`2`，派生订单（字段复制、`status=0`），按月生成 12 期账单（`periodNo`、`dueDate=起租日+i 月`、`amount=月租`），房源置 `5`，通知租客 | FR-18/FR-19 |
| UT-CONTRACT-10 | 期数下限 | 起止不足一个月仍生成 1 期 | FR-19 |
| UT-CONTRACT-11 | 乱序/重复签署 | 房东抢先签、租客重复签、生效后再签 → `3003` | FR-18 |
| UT-CONTRACT-12 | 拒签 | 待签阶段双方可作废（→`4`）；生效后或第三方 → `3003`/`1007` | FR-18 |
| UT-CONTRACT-13 | 退租 | `2`→`3`，订单关闭（`status=1`），房源重新上架（`3`），通知对方 | FR-19（验收：退租更新房源状态） |
| UT-CONTRACT-14 | 未知动作 | `1000` | FR-18 |
| UT-CONTRACT-15 | 账单可见性 | 仅订单双方或管理员可见，他人 `3004` | NFR-03 |
| UT-CONTRACT-16 | 账单支付权限 | 仅订单租客可支付（房东 `1007`）；非待支付状态 `1000`；成功后通知房东（type=4） | FR-19 |
| UT-CONTRACT-17 | 订单列表口径 | 租客按 `tenant_id`、房东按 `landlord_id` 过滤（校验真实列名） | FR-19 |
| UT-CONTRACT-18 | 我的合同 | 按签署双方过滤（`tenant_id` 或 `landlord_id`） | FR-18 |
| UT-CONTRACT-19 | 合同详情不存在 | `3004` | FR-18 |
| UT-CONTRACT-20 | 签署时间戳 | 双方签署时间各自独立写回，房东签署时间不早于租客 | FR-18 |

### 2.5 UT-REVIEW 评价（FR-20）— `ReviewServiceTest`，7 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-REVIEW-01 | 仅订单租客可评价 | 订单不存在或非本人 → `3005`，不落库 | FR-20 |
| UT-REVIEW-02 | 仅完成合同可评价 | 订单 `status=0`（在租）→ `3005` +「合同履行完成后方可评价」 | FR-20（验收：仅完成合同可评价） |
| UT-REVIEW-03 | 一单一评 | 重复评价被 `uk_order` 拦截 → `3005` +「该订单已评价过」 | FR-20 |
| UT-REVIEW-04 | 评价成功 | 字段取订单快照，`status=0`，通知房东（type=9） | FR-20/FR-21 |
| UT-REVIEW-05 | 均分回填 | 多评价取均值，HALF_UP 保留 1 位写入 `house.avg_score` | FR-20/FR-11 |
| UT-REVIEW-06 | 无有效评价 | 写 `null` 而非 `0`（避免 0 分误导推荐与展示） | FR-11 |
| UT-REVIEW-07 | 评价列表 | 仅 `status=0` 展示，租客名脱敏为「租客+昵称」 | FR-20 |

### 2.6 UT-AI AI 分析（FR-08/14/15/16）— `AnalysisServiceTest`，21 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-AI-01 | 定价区间 | 均值 `setScale(2)`；`low=avg×0.92`、`high=avg×1.08` 取整；依据含样本量 | FR-15 |
| UT-AI-02 | 取整方式 | `1932.3036→1932`、`2268.3564→2268`（HALF_UP） | FR-15 |
| UT-AI-03 | 样本回退 | 同小区 <3 条 → 同区域同户型，依据文案切换 | FR-15 |
| UT-AI-04 | 无样本兜底 | 区间与均价为 `null`、`sampleCount=0`，提示人工定价 | FR-15（验收：明确告知数据不足） |
| UT-AI-05 | 样本较少提示 | `<3` 条提示「样本较少，建议区间仅供参考」 | FR-15 |
| UT-AI-06 | 结果留痕 | `ai_analysis` 落库 `type=1`，`model` 记录当前引擎标识 | NFR-05 |
| UT-AI-07 | 落库失败降级 | 写库异常不影响定价结果返回 | NFR-03/NFR-07 |
| UT-AI-08 | 列宽截断 | 引擎标识超长截断到 100 字符（防整条写不进） | NFR-04 |
| UT-AI-09 | 租金偏离规则 | 同区域样本 ≥3 且租金 < 均价 60% → +40 | FR-16 |
| UT-AI-10 | 引流话术规则 | 六个关键词每条 +20，叠加后封顶 100 | FR-16 |
| UT-AI-11 | 完整度与导流 | 描述 <50 字 +15；含手机号/微信 +25 | FR-16 |
| UT-AI-12 | 低风险判定 | 无异常特征 → 0 分、无疑点 | FR-16 |
| UT-AI-13 | 样本不足不判偏离 | peers <3 时不做租金偏离判定 | FR-16 |
| UT-AI-14 | 检测不存在房源 | `2001` | FR-16 |
| UT-AI-15 | 解读权限 | 合同不存在 `3004`；非合同双方 `1007` | FR-14/NFR-03 |
| UT-AI-16 | 条款解析失败 | 坏 JSON → `4001` | FR-14 |
| UT-AI-17 | 风险条款识别 | 「押金不予退还/单方涨租/自动续约」命中；含「甲方违约」的违约金条款不标红 | FR-14（验收：3 处风险样例全部命中） |
| UT-AI-18 | 条款通俗化 | 乙方→你（租客）、甲方→房东，并按押付/维修/续租追加解释 | FR-14 |
| UT-AI-19 | 风险下标回写 | `type=3` 落库并向合同写回 `risk_flags=[0]` | FR-14/NFR-05 |
| UT-AI-20 | 智能识别填充 | 标题/图片名关键词 → 设施、朝向、楼层推断 | FR-08 |
| UT-AI-21 | 无关键词兜底 | 设施取默认组合，描述可一键填充且可改 | FR-08 |

### 2.7 UT-KB 知识库与检索（FR-13）— `KbServiceTest`，12 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-KB-01 | 空查询 | 无关键词 | FR-13 |
| UT-KB-02 | 停用词剔除 | 「请问/怎么」等被移除，剩余短段整体保留 | FR-13 |
| UT-KB-03 | 2 字滑窗切词 | 长片段切 2 字窗口，最多 8 个关键词 | FR-13 |
| UT-KB-04 | 标点分隔 | 中英文标点与符号视作分隔符 | FR-13 |
| UT-KB-05 | 无关键词不查库 | 直接返回空列表 | FR-13 |
| UT-KB-06 | 命中排序 | 按**命中关键词个数**降序，截断 topK | FR-13（验收：回答准确率依赖命中质量） |
| UT-KB-07 | 无命中 | 返回空列表（→ 转人工） | FR-13 |
| UT-KB-08 | 引用组装 | 文档缺失时标题兜底「知识库」；片段 >120 字截断加省略号 | FR-13/NFR-05 |
| UT-KB-09 | 命中判定 | `hit()` 即"检索 top1 是否为空"，客服转人工依据 | FR-13 |
| UT-KB-10 | 文档切片 | 220 字切片、优先在句号处切分、片序号与字数回写 | FR-13 |
| UT-KB-11 | 短文档 | 不切片，单片段 | FR-13 |
| UT-KB-12 | 无标点长文档 | 固定长度顺序切片，拼接等于原文（无内容丢失） | FR-13 |

### 2.8 UT-SEC 鉴权链路（NFR-02/NFR-03）— `SecurityGuardTest`，11 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-SEC-01 | JWT 往返 | 校验出 `sub=uid`、`role`、`jti`、过期时间 | NFR-03 |
| UT-SEC-02 | 篡改 token | 改尾字符/追加字符/非 JWT 串均校验失败 | NFR-03 |
| UT-SEC-03 | 伪造签名 | 换密钥签发的 token 校验失败 | NFR-03 |
| UT-SEC-04 | 过期 token | 校验失败 | NFR-03 |
| UT-SEC-05 | 无角色注解 | 匿名放行 | NFR-03 |
| UT-SEC-06 | 未登录访问受限接口 | `1002` +「请先登录」 | NFR-03 |
| UT-SEC-07 | 角色不符 | `1007` | NFR-03（垂直越权） |
| UT-SEC-08 | 类级/方法级注解 | 类级对全部方法生效；方法级支持多角色 `{2,3}` | NFR-03 |
| UT-SEC-09 | 非控制器方法 | 静态资源等直接放行 | NFR-03 |
| UT-SEC-10 | 未登录读上下文 | `userId()` → `1007`；`role()` → `null` | NFR-03 |
| UT-SEC-11 | 上下文清理 | `set` 可读、`clear` 后不残留（ThreadLocal 防串号） | NFR-03 |

### 2.9 UT-CRYPTO 敏感信息加密（NFR-04）— `CryptoUtilTest`，6 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-CRYPTO-01 | 加解密往返 | 解密还原明文 | NFR-04 |
| UT-CRYPTO-02 | 密文随机化 | 同一明文两次密文不同（随机 IV），均能解密 | NFR-04 |
| UT-CRYPTO-03 | 哈希标准向量 | `sha256("abc")=ba7816bf…15ad`，且结果稳定 | NFR-04 |
| UT-CRYPTO-04 | 完整性校验 | 篡改密文 → GCM tag 校验失败并抛异常 | NFR-04 |
| UT-CRYPTO-05 | 非法密文 | 抛出明确异常，不返回脏数据 | NFR-04 |
| UT-CRYPTO-06 | UTF-8 内容 | 中文姓名等无损往返 | NFR-04 |

### 2.10 UT-DTO 请求参数约束（NFR-03）— `DtoValidationTest`，13 条

| 编号 | 被测对象 | 关键约束 | 追溯 |
| ---- | -------- | -------- | ---- |
| UT-DTO-01 | `AuthDto.RegisterReq` | 手机号 11 位数字、验证码必填、密码 6~32、角色必填 | FR-01 |
| UT-DTO-02 | 同上 | 现状记录：`role` 无取值域约束（仅前端限制 1/2） | FR-01（见 §五 遗留项） |
| UT-DTO-03 | `AuthDto.LoginReq` | 用户名与密码必填 | FR-02 |
| UT-DTO-04 | `AuthDto.ChangePasswordReq` | 新密码 6~32 | FR-04 |
| UT-DTO-05 | `AuthDto.RealnameReq` | 姓名必填、身份证 `\d{17}[\dXx]` | FR-03 |
| UT-DTO-06 | `HouseDto.SaveReq` | 面积 ≥1、租金 1~1000000、经度 ±180、纬度 ±90、经纬度必填 | FR-05 |
| UT-DTO-07 | 同上 | 标题/小区/城市/区必填（空串与空白串均拦截） | FR-05 |
| UT-DTO-08 | `TradeDto.AppointmentCreateReq` | 房源 id 与到访时间必填 | FR-17 |
| UT-DTO-09 | `TradeDto.ContractCreateReq` | 房源 id 与起止日期必填 | FR-18 |
| UT-DTO-10 | `TradeDto.ReviewReq` | 订单 id 必填；两项评分 1~5（0/6/null 均拦截） | FR-20 |
| UT-DTO-11 | `TradeDto.ReportReq` | 举报类型、目标 id 必填，原因不可为空 | FR-23 |
| UT-DTO-12 | `AiDto.SessionCreateReq` | 场景值 1~3 | FR-12 |
| UT-DTO-13 | `AiDto.MessageSendReq` | 消息内容必填 | FR-12 |

### 2.11 UT-API 统一响应与异常处理（NFR-03）— `ApiContractTest`，5 条

| 编号 | 被测规则 | 预期 | 追溯 |
| ---- | -------- | ---- | ---- |
| UT-API-01 | 成功响应 | `code=0`、`message="ok"`、`data` 为业务数据 | NFR-08 |
| UT-API-02 | 业务异常透传 | 原错误码与文案，`data=null` | NFR-03 |
| UT-API-03 | 复用码区分语义 | 如 `3005` 下按 message 区分「未完成合同」与「已评价过」 | NFR-03 |
| UT-API-04 | 参数校验失败 | 统一 `1000` 且 message 含字段名与提示 | NFR-03 |
| UT-API-05 | 未预期异常 | 统一 `5000`，不外泄内部信息（如连接串） | NFR-03/NFR-07 |

### 2.12 既有用例（本次纳入回归）— 9 条

| 测试类 | 条数 | 覆盖内容 |
| ------ | ---- | -------- |
| `AiPropsTest` | 6 | 多后端（协议）解析优先级、缺 Key 不静默换厂商、协议别名归一 |
| `OpenAiResponsesChatModelTest` | 3 | Responses 协议请求体映射、工具扁平化、多轮工具消息与响应解析 |

---

## 三、前端单元测试用例

### 3.1 FT-RUNTIME 运行时适配（一套代码两种分发）— `runtime.test.ts`，5 条

| 编号 | 被测规则 | 预期 |
| ---- | -------- | ---- |
| FT-RUNTIME-01 | 浏览器版（无注入） | `API_BASE=''`，`apiUrl('/houses')='/api/v1/houses'`，资源走同源相对路径 |
| FT-RUNTIME-02 | 桌面端（preload 注入） | 接口与资源均补全为绝对地址（`file://` 下相对路径会指向本地文件） |
| FT-RUNTIME-03 | 基址规范化 | 尾部多斜杠被去除，不产生 `//` |
| FT-RUNTIME-04 | 优先级 | 注入值 > `VITE_API_BASE` |
| FT-RUNTIME-05 | 资源地址 | 空值返回空串；`http(s)/data/blob` 绝对地址原样直通 |

### 3.2 FT-DICT / FT-FMT 字典与格式化 — `constants.test.ts`，9 条

| 编号 | 被测规则 | 预期 |
| ---- | -------- | ---- |
| FT-DICT-01 | 房源状态字典 | 0~5 文案与库表注释一一对应 |
| FT-DICT-02 | 预约/合同/订单/账单字典 | 键集合与后端状态机取值完全一致 |
| FT-DICT-03 | 通知类型字典 | 覆盖后端使用的 type（1/2/3/4/9） |
| FT-DICT-04 | 行政区坐标 | 每个行政区都有地图中心坐标，且经纬度在大连范围内 |
| FT-DICT-05 | 表单选项集合 | 非空、无重复，设施标签与后端识别体系一致 |
| FT-FMT-01 | 金额空值 | 占位 `-`（不显示 0） |
| FT-FMT-02 | 金额格式 | `¥2,100`，兼容字符串数字 |
| FT-FMT-03 | 时间格式 | `2026-09-17T14:30:25 → 2026-09-17 14:30` |
| FT-FMT-04 | 时间空值 | 占位 `-` |

### 3.3 FT-SSE AI 对话流式解析（FR-12/NFR-02）— `api.test.ts`，6 条

| 编号 | 被测规则 | 预期 |
| ---- | -------- | ---- |
| FT-SSE-01 | delta/done 事件 | delta 按到达顺序逐字回调，done 收尾并携带 messageId/引用/时延 |
| FT-SSE-02 | 半行缓冲 | 单条事件被拆到多个网络分片时仍正确拼装（流式渲染不花屏） |
| FT-SSE-03 | 容错 | 注释行、非 `data:` 行、坏 JSON 行被忽略且不中断流 |
| FT-SSE-04 | HTTP 失败 | 回调 `连接智能助手失败（4002）`，不触发 delta |
| FT-SSE-05 | 网络异常 | 回调「网络连接失败」且 Promise 正常结束（不抛未捕获异常） |
| FT-SSE-06 | 请求构造 | URL 含版本前缀 `/api/v1`、携带 Bearer JWT 与 JSON 头 |

---

## 四、用例统计与追溯

| 模块 | 用例数 | 覆盖需求 |
| ---- | ------ | -------- |
| 后端：认证与用户 | 13 | FR-01/02/03/04、FR-22、NFR-04 |
| 后端：房源 | 19 | FR-05/06/07/11 |
| 后端：预约 | 12 | FR-17、FR-21 |
| 后端：签约/订单/账单 | 20 | FR-18/19、FR-21 |
| 后端：评价 | 7 | FR-20、FR-11 |
| 后端：AI 分析 | 21 | FR-08/14/15/16、NFR-05 |
| 后端：知识库 | 12 | FR-13 |
| 后端：鉴权链路 | 11 | NFR-02/NFR-03 |
| 后端：加密 | 6 | NFR-04 |
| 后端：参数约束 | 13 | FR-01/03/05/12/17/18/20/23 |
| 后端：响应与异常 | 5 | NFR-03/NFR-07/NFR-08 |
| 后端：既有回归 | 9 | NFR-09 |
| 前端：运行时/字典/SSE | 20 | NFR-02/NFR-06、FR-12 |
| **合计** | **168** | — |

需求级覆盖情况与"未覆盖项及后续计划"统一列在《测试用例设计》第四节，
本层不重复；本层未覆盖而必须靠真实组件验证的部分（唯一约束、聚合 SQL、TTL、流式真实模型）
均已在集成层落地。

---

## 五、首轮执行发现的缺陷

| 编号 | 严重度 | 位置 | 现象 | 根因 | 处理 |
| ---- | ------ | ---- | ---- | ---- | ---- |
| BUG-01 | 中（功能缺陷，不涉数据泄漏） | `common/CryptoUtil.java` `decrypt` | 身份证号解密恒失败；受其影响 `GET /users/me/realname` 返回的 `maskedIdCard` 恒为 `****`（`AuthService.mask` 捕获异常后兜底） | GCM 参数构造把**密文**当成了 IV（`new GCMParameterSpec(128, all, 12, all.length-12)` 的 offset/len 语义是 IV 的位置与长度），且 `doFinal(all)` 又把 IV 一并当作密文，必然 `AEADBadTagException: Tag mismatch` | 已修复：IV 取前 12 字节、`doFinal(all, 12, len-12)`；回归用例 `UT-CRYPTO-01/02/04/06`、`UT-AUTH-12`，集成层 `IT-09-05` |

> 说明：该缺陷此前未被发现的原因是加密**写入**正常、且 `decrypt` 只有脱敏展示一个调用点，
> 异常被兜底文案掩盖；单测以真实密钥驱动加解密往返后立即暴露。

---

## 六、执行方式

```bash
# 后端单元测试（无需 MySQL/Redis）
cd server && mvn test

# 前端单元测试
cd frontend && npm test

# 单类/单方法执行
cd server && mvn test -Dtest=ContractServiceTest
cd frontend && npx vitest run src/api.test.ts
```

CI 中的对应步骤见《CI 流水线设计》：`backend-test` 作业执行 `mvn -B -ntp test`，
`frontend-test` 作业执行 `npm test` 与 `npm run build:web`（含 `tsc --noEmit` 严格类型检查）。
